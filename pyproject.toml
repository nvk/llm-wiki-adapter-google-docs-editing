[project]
name = "llm-wiki-adapter-google-docs-editing"
version = "0.8.9"
requires-python = ">=3.9"
description = "Google Docs tracked-suggestions adapter for llm-wiki"

[tool.pytest.ini_options]
testpaths = ["tests"]
