[project]
name = "llm-wiki-adapter-google-docs-editing"
version = "0.6.0"
requires-python = ">=3.9"
description = "Private content-free Google Docs tracked-changes adapter"

[tool.pytest.ini_options]
testpaths = ["tests"]
